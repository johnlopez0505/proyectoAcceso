extends layout

block content
    h1 Bienvenido, #{user || 'invitado'}
    if user 
        p 
            a(href = "/logout") Cerrar sesión 
    else
        p
            a(href = "/login") Iniciar Sesión 

        
    
            