html 
    head
       title = pageTitle
    link(href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" 
    integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous")
    body
        nav.p-0.bg-success.bg-gradient(class="container pagination" style="width: 90%; margin-top: 20px;")
            ul(class="nav justify-content-center" style="color: black; width: 100%;")
                li.nav-item(style="width: 14.286%;")
                    a.nav-link(href = "/" class="text-dark btn btn-warning" data-page="/") Inicio
                if user
                    li.nav-item(style="width: 14.286%;") 
                        a.nav-link(href = "/alumnos" class="text-dark" 
                            class="btn btn-warning" data-page="/alumnos") Alumnos
                    li.nav-item(style="width: 14.286%;")
                        a.nav-link(href = "/profesores" class="text-dark" 
                            class="btn btn-warning" data-page="/profesores") Profesores
                    li.nav-item(style="width: 14.286%;") 
                        a.nav-link(href = "/asignaturas" class="text-dark" 
                            class="btn btn-warning" data-page="/asignaturas") Asignaturas
                    li.nav-item(style="width: 14.286%;")
                        a.nav-link(href = "/matricular" class="text-dark" 
                            class="btn btn-warning" data-page="/matricular") Matriculas
                    li.nav-item(style="width: 14.286%;")
                        a.nav-link(href = "/asignar-profesores" class="text-dark" 
                            class="btn btn-warning" data-page="/asignar-profesores") Asignar Profesores
                    li.nav-item(style="width: 14.286%;")
                        a.nav-link(href = "/logout" class="text-dark" 
                            class="btn btn-warning" data-page="/logout") Cerrar sesión
                else
                    li.nav-item(style="width: 14.286%;") 
                        a.nav-link(href = "/login" class="text-dark" 
                            class="btn btn-warning" data-page="/login") Iniciar Sesión

        script.
            document.addEventListener('DOMContentLoaded', function () {
                // Obtener la página actual 
                const currentPage = window.location.pathname;
                // obtener todos los elementos con el atributo data-page
                const navLinks = document.querySelectorAll('[data-page]');
                // Itera sobre los elementos y agrega la clase active si coincide con la página actual
                navLinks.forEach(function (link) {
                    const page = link.getAttribute('data-page');
                    if (currentPage === page) {
                        link.classList.add('active');
                    }
                });
            });

        block content 

        footer.p-0.bg-success.bg-gradient(class="container mx-auto text-center" 
            style="width: 90%; margin-top: 20px;") 
            p IES Virgen del Carmen (2023)